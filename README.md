# GolangFramework
Golang Clean Architecture Framework

# Author : Nolan

## 📊 完整流程圖（視覺化）
```
┌─────────────────────────────────────────────────┐
│ 1. main.go 啟動伺服器                            │
│    - 載入配置                                    │
│    - 連接資料庫                                  │
│    - 組裝所有組件（依賴注入）                    │
│    - 啟動 HTTP 伺服器監聽 :8080                 │
└─────────────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────┐
│ 用戶發送請求：PUT /api/v1/users/profile         │
│ Header: Authorization: Bearer {token}           │
│ Body: {"username": "new_name"}                  │
└─────────────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────┐
│ 2. Router 路由匹配                               │
│    - 匹配到 PUT /users/profile                  │
│    - 發現需要經過 AuthMiddleware                │
│    - 將請求交給 Middleware 鏈                   │
└─────────────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────┐
│ 3. Middleware 鏈處理                             │
│    ① Recovery    - 捕捉 Panic                   │
│    ② Logger      - 記錄請求                     │
│    ③ ErrorHandler- 處理錯誤                     │
│    ④ Auth        - 驗證 JWT Token               │
│       - 解析 Token                              │
│       - 驗證有效性                              │
│       - 存入 user_id 到 Context                 │
│       - c.Next() 繼續下一步                     │
└─────────────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────┐
│ 4. Handler 處理 HTTP                             │
│    userHandler.UpdateProfile(c)                 │
│    - 從 Context 取得 user_id                    │
│    - 解析 JSON Body                             │
│    - 驗證輸入格式                               │
│    - 呼叫 userUseCase.UpdateUser()              │
└─────────────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────┐
│ 5. UseCase 執行業務邏輯                          │
│    userUseCase.UpdateUser(ctx, userID, req)     │
│    - 透過 userRepo.GetByID() 取得當前資料       │
│    - 檢查 Email 是否重複（業務規則）            │
│    - 檢查 Username 是否重複（業務規則）         │
│    - 透過 userRepo.Update() 儲存               │
│    - 轉換成 Response DTO                        │
└─────────────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────┐
│ 6. Repository 操作資料庫                         │
│    userRepo.Update(ctx, user)                   │
│    - 轉換 Entity → SQLC Params                  │
│    - 呼叫 queries.UpdateUser()                  │
│    - 執行 SQL UPDATE 語句                       │
└─────────────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────┐
│ 7. PostgreSQL 資料庫                             │
│    UPDATE users SET ... WHERE id = 1            │
│    RETURNING *;                                 │
│    - 更新資料                                   │
│    - 返回更新後的資料                           │
└─────────────────────────────────────────────────┘
                       ↓ 返回
┌─────────────────────────────────────────────────┐
│ Repository 轉換資料                              │
│    SQLC Model → Entity                          │
└─────────────────────────────────────────────────┘
                       ↓ 返回
┌─────────────────────────────────────────────────┐
│ UseCase 轉換資料                                 │
│    Entity → Response DTO                        │
└─────────────────────────────────────────────────┘
                       ↓ 返回
┌─────────────────────────────────────────────────┐
│ Handler 包裝 HTTP 響應                           │
│    utils.SuccessResponse(...)                   │
│    生成 JSON 響應                                │
└─────────────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────┐
│ Middleware 記錄響應日誌                          │
│    [GIN] 2025/12/02 | 200 | 50ms | PUT /users/profile
└─────────────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────┐
│ HTTP Response 返回給用戶                         │
│ Status: 200 OK                                  │
│ Body: {                                         │
│   "success": true,                              │
│   "message": "更新成功",                        │
│   "data": {...}                                 │
│ }                                               │
└─────────────────────────────────────────────────┘